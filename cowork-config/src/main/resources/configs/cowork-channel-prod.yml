server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/cowork_channel?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    hikari:
      # Tomcat 처리 스레드가 동시에 요청하는 connection 수를 상회하도록 잡되,
      # MySQL max_connections를 서비스 인스턴스 수로 나눈 값을 넘지 않게 유지한다.
      maximum-pool-size: ${DB_POOL_MAX_SIZE:20}
      minimum-idle: ${DB_POOL_MIN_IDLE:5}
      # pool 고갈 시 무한 대기 대신 빠르게 실패시켜 스레드 점유가 번지지 않게 한다.
      connection-timeout: 3000
      validation-timeout: 1000
      idle-timeout: 600000
      max-lifetime: 1500000
      keepalive-time: 120000
      pool-name: channel-hikari
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://cowork-config:8761/eureka/}
  instance:
    initial-status: STARTING

management:
  endpoint:
    health:
      probes:
        enabled: true
      group:
        readiness:
          include: readinessState,projectionReadiness

projection:
  readiness:
    poll-interval-ms: 1000

account-share:
  encryption-key: ${ACCOUNT_CREDENTIAL_ENCRYPTION_KEY}
  oauth:
    # 외부 제공자 지연이 무한정 이어지지 않도록 상한을 둔다. OAuth 콜백은
    # transaction 밖에서 호출되지만, 상한이 없으면 요청 스레드가 묶인다.
    connect-timeout-ms: 3000
    read-timeout-ms: 5000
    callback-base-url: ${PUBLIC_API_BASE_URL}
    client-redirect-url: ${PUBLIC_WEB_ORIGIN}
    state-secret: ${ACCOUNT_SHARE_OAUTH_STATE_SECRET}
    github:
      client-id: ${GITHUB_ACCOUNT_SHARE_CLIENT_ID:}
      client-secret: ${GITHUB_ACCOUNT_SHARE_CLIENT_SECRET:}
      token-url: https://github.com/login/oauth/access_token
      userinfo-url: https://api.github.com/user
      scope: read:user,user:email
    notion:
      client-id: ${NOTION_ACCOUNT_SHARE_CLIENT_ID:}
      client-secret: ${NOTION_ACCOUNT_SHARE_CLIENT_SECRET:}
      token-url: https://api.notion.com/v1/oauth/token
      userinfo-url: https://api.notion.com/v1/users/me
      scope: ""
    jira:
      client-id: ${JIRA_ACCOUNT_SHARE_CLIENT_ID:}
      client-secret: ${JIRA_ACCOUNT_SHARE_CLIENT_SECRET:}
      token-url: https://auth.atlassian.com/oauth/token
      userinfo-url: https://api.atlassian.com/me
      scope: read:me
    google:
      client-id: ${GOOGLE_ACCOUNT_SHARE_CLIENT_ID:}
      client-secret: ${GOOGLE_ACCOUNT_SHARE_CLIENT_SECRET:}
      token-url: https://oauth2.googleapis.com/token
      userinfo-url: https://www.googleapis.com/oauth2/v3/userinfo
      scope: openid email profile
    facebook:
      client-id: ${FACEBOOK_ACCOUNT_SHARE_CLIENT_ID:}
      client-secret: ${FACEBOOK_ACCOUNT_SHARE_CLIENT_SECRET:}
      token-url: https://graph.facebook.com/v18.0/oauth/access_token
      userinfo-url: https://graph.facebook.com/me?fields=id,email
      scope: email
