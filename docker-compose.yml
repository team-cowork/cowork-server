services:
  mysql:
    image: mysql:9.7.1
    container_name: cowork-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "${MYSQL_HOST_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init.sh:/docker-entrypoint-initdb.d/init.sh
    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:18.4
    container_name: cowork-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./docker/postgres/init.sh:/docker-entrypoint-initdb.d/init.sh
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  mongodb:
    image: mongo:8.3
    container_name: cowork-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: [ "CMD", "mongosh", "--eval", "db.adminCommand('ping')" ]
      interval: 10s
      timeout: 5s
      retries: 5

  kafka:
    image: apache/kafka:4.3.0
    container_name: cowork-kafka
    restart: unless-stopped
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:9094
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      # Topics are provisioned explicitly by kafka-init so cold starts do not
      # depend on broker auto-creation timing. This also matches production
      # clusters where automatic topic creation is normally disabled.
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qg
    ports:
      - "${KAFKA_EXTERNAL_HOST_PORT:-9094}:9094"
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: [ "CMD", "/opt/kafka/bin/kafka-topics.sh", "--bootstrap-server", "localhost:9092", "--list" ]
      interval: 15s
      timeout: 10s
      retries: 5

  kafka-init:
    image: apache/kafka:4.3.0
    container_name: cowork-kafka-init
    restart: "no"
    depends_on:
      kafka:
        condition: service_healthy
    environment:
      KAFKA_TOPIC_PARTITIONS: ${KAFKA_TOPIC_PARTITIONS:-1}
      KAFKA_TOPIC_REPLICATION_FACTOR: ${KAFKA_TOPIC_REPLICATION_FACTOR:-1}
      KAFKA_TOPICS: >-
        springCloudBus
        user.data.sync
        user.lifecycle
        team.lifecycle
        channel.member.event
        channel.event
        project.event
        chat.message
        github.issue.create
        github.issue.result
        notification.trigger
        voice.event
        preference.status.changed
        preference.team.setting.changed
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        for topic in $${KAFKA_TOPICS}; do
          /opt/kafka/bin/kafka-topics.sh \
            --bootstrap-server kafka:9092 \
            --create \
            --if-not-exists \
            --topic "$${topic}" \
            --partitions "$${KAFKA_TOPIC_PARTITIONS}" \
            --replication-factor "$${KAFKA_TOPIC_REPLICATION_FACTOR}"
        done

  kafka-ui:
    # The project does not publish immutable release tags consistently; pin the
    # verified multi-architecture image digest instead of following latest.
    image: provectuslabs/kafka-ui@sha256:8f2ff02d64b0a7a2b71b6b3b3148b85f66d00ec20ad40c30bdcd415d46d31818
    container_name: cowork-kafka-ui
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
    environment:
      KAFKA_CLUSTERS_0_NAME: cowork-local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
    ports:
      - "8090:8080"

  vault:
    image: hashicorp/vault:2.0.3
    container_name: cowork-vault
    restart: unless-stopped
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: dev-root-token
      VAULT_DEV_LISTEN_ADDRESS: 0.0.0.0:8200
      SKIP_SETCAP: "true"
    ports:
      - "8200:8200"
    cap_add:
      - IPC_LOCK
      - SETFCAP
    healthcheck:
      test: [ "CMD-SHELL", "wget -qO- http://127.0.0.1:8200/v1/sys/health || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5

  vault-init:
    image: hashicorp/vault:2.0.3
    container_name: cowork-vault-init
    restart: "no"
    depends_on:
      vault:
        condition: service_healthy
    environment:
      VAULT_ADDR: http://cowork-vault:8200
      VAULT_TOKEN: dev-root-token
      JWT_SECRET: ${JWT_SECRET}
      SECRET_KEY_BASE: ${SECRET_KEY_BASE}
      DATAGSM_CLIENT_ID: ${DATAGSM_CLIENT_ID}
      DATAGSM_WEBHOOK_SECRET: ${DATAGSM_WEBHOOK_SECRET:-}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      MONGO_ROOT_USERNAME: ${MONGO_ROOT_USERNAME}
      MONGO_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY:-devkey}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET:-devsecret}
      DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL:-}
      ACCOUNT_CREDENTIAL_ENCRYPTION_KEY: ${ACCOUNT_CREDENTIAL_ENCRYPTION_KEY}
      ACCOUNT_SHARE_OAUTH_STATE_SECRET: ${ACCOUNT_SHARE_OAUTH_STATE_SECRET}
      GITHUB_ACCOUNT_SHARE_CLIENT_ID: ${GITHUB_ACCOUNT_SHARE_CLIENT_ID:-}
      GITHUB_ACCOUNT_SHARE_CLIENT_SECRET: ${GITHUB_ACCOUNT_SHARE_CLIENT_SECRET:-}
      NOTION_ACCOUNT_SHARE_CLIENT_ID: ${NOTION_ACCOUNT_SHARE_CLIENT_ID:-}
      NOTION_ACCOUNT_SHARE_CLIENT_SECRET: ${NOTION_ACCOUNT_SHARE_CLIENT_SECRET:-}
      JIRA_ACCOUNT_SHARE_CLIENT_ID: ${JIRA_ACCOUNT_SHARE_CLIENT_ID:-}
      JIRA_ACCOUNT_SHARE_CLIENT_SECRET: ${JIRA_ACCOUNT_SHARE_CLIENT_SECRET:-}
      GOOGLE_ACCOUNT_SHARE_CLIENT_ID: ${GOOGLE_ACCOUNT_SHARE_CLIENT_ID:-}
      GOOGLE_ACCOUNT_SHARE_CLIENT_SECRET: ${GOOGLE_ACCOUNT_SHARE_CLIENT_SECRET:-}
      FACEBOOK_ACCOUNT_SHARE_CLIENT_ID: ${FACEBOOK_ACCOUNT_SHARE_CLIENT_ID:-}
      FACEBOOK_ACCOUNT_SHARE_CLIENT_SECRET: ${FACEBOOK_ACCOUNT_SHARE_CLIENT_SECRET:-}
      GITHUB_APP_INTERNAL_API_KEY: ${GITHUB_APP_INTERNAL_API_KEY:-}
    volumes:
      - ./cowork-config/src/main/resources/vault/vault-init.sh:/scripts/vault-init.sh:ro
    entrypoint: ["/bin/sh", "/scripts/vault-init.sh"]

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: cowork-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "9000:9000"
      - "9002:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: [ "CMD", "mc", "ready", "local" ]
      interval: 10s
      timeout: 5s
      retries: 5

  minio-init:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    container_name: cowork-minio-init
    restart: "no"
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET: ${MINIO_BUCKET:-cowork-bucket}
    volumes:
      - ./docker/minio/cors.json:/cors.json:ro
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD};
      mc mb --ignore-existing local/$${MINIO_BUCKET};
      mc cors set local/$${MINIO_BUCKET} /cors.json;
      exit 0;
      "

  elasticsearch:
    build:
      context: ./docker
      dockerfile: elasticsearch.dockerfile
    container_name: cowork-elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    ports:
      - "9200:9200"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9200/_cluster/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 60s

  redis:
    image: redis:8.8.0-alpine
    container_name: cowork-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  livekit:
    image: livekit/livekit-server:v1.13.3
    container_name: cowork-livekit
    restart: unless-stopped
    environment:
      LIVEKIT_API_KEY: ${LIVEKIT_API_KEY:-devkey}
      LIVEKIT_API_SECRET: ${LIVEKIT_API_SECRET:-devsecret}
      LIVEKIT_USE_EXTERNAL_IP: ${LIVEKIT_USE_EXTERNAL_IP:-false}
    ports:
      - "7880:7880"
      - "7881:7881"
    volumes:
      - ./docker/livekit/${LIVEKIT_CONFIG_FILE:-livekit.yaml}:/etc/livekit.yaml:ro
    command: --config /etc/livekit.yaml
    healthcheck:
      test: [ "CMD-SHELL", "wget -qO- http://localhost:7880 || exit 1" ]
      interval: 10s
      timeout: 5s
      retries: 5


  loki:
    image: grafana/loki:3.7.2
    container_name: cowork-loki
    restart: unless-stopped
    ports:
      - "3100:3100"
    volumes:
      - ./cowork-monitoring/loki/loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki_data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    healthcheck:
      # The official Loki image is distroless and does not contain /bin/sh or wget.
      # Container liveness is provided by Docker; validate the loaded config without
      # depending on utilities that are absent from the production image.
      test: ["CMD", "/usr/bin/loki", "-verify-config", "-config.file=/etc/loki/local-config.yaml"]
      interval: 10s
      timeout: 5s
      retries: 5

  promtail:
    image: grafana/promtail:3.6.0
    container_name: cowork-promtail
    restart: unless-stopped
    volumes:
      - ./cowork-monitoring/promtail/promtail-config.yml:/etc/promtail/config.yml:ro
      - cowork_logs:/var/log/cowork:ro
      - promtail_positions:/tmp
    command: -config.file=/etc/promtail/config.yml -config.expand-env=true
    depends_on:
      loki:
        condition: service_healthy

  blackbox_exporter:
    image: prom/blackbox-exporter:v0.28.0
    container_name: cowork-blackbox-exporter
    restart: unless-stopped
    ports:
      - "9115:9115"
    volumes:
      - ./cowork-monitoring/blackbox/blackbox.yml:/etc/blackbox_exporter/config.yml:ro
    command:
      - '--config.file=/etc/blackbox_exporter/config.yml'

  # TODO: 임시 변통 - 홈서버 등 Eureka 미등록 외부 호스트를 Prometheus가 스크랩/probe할 수 있도록
  # EXTERNAL_HOST_URL 환경변수를 기반으로 file_sd 대상 json을 생성한다. Prometheus는 원래
  # config.expand-env 같은 환경변수 치환 기능이 없어 file_sd 파일을 미리 만들어두는 방식으로 우회한다.
  # ExternalRouteConfig(gateway)를 제거할 때 이 서비스와 sd/blackbox, sd/external-home-metrics.json도 함께 정리할 것.
  prometheus-external-target-writer:
    image: busybox:1.36
    container_name: cowork-prometheus-external-target-writer
    environment:
      EXTERNAL_HOST_URL: ${EXTERNAL_HOST_URL:-}
      EXTERNAL_HOST_NAME: ${EXTERNAL_HOST_NAME:-external}
      EXTERNAL_HOST_HEALTH_PATH: ${EXTERNAL_HOST_HEALTH_PATH:-/health}
    volumes:
      - ./cowork-monitoring/prometheus/sd:/sd
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        mkdir -p /sd/blackbox
        if [ -n "$$EXTERNAL_HOST_URL" ]; then
          url_trimmed="$${EXTERNAL_HOST_URL%/}"
          host_port="$${url_trimmed#*://}"
          printf '[{"targets":["%s%s"],"labels":{}}]\n' "$$url_trimmed" "$$EXTERNAL_HOST_HEALTH_PATH" > /sd/blackbox/external-home.json
          printf '[{"targets":["%s"],"labels":{"service":"%s","__metrics_path__":"/actuator/prometheus"}}]\n' "$$host_port" "$$EXTERNAL_HOST_NAME" > /sd/external-home-metrics.json
        else
          printf '[]\n' > /sd/blackbox/external-home.json
          printf '[]\n' > /sd/external-home-metrics.json
        fi
    restart: "no"

  prometheus:
    image: prom/prometheus:v3.12.0
    container_name: cowork-prometheus
    restart: unless-stopped
    depends_on:
      prometheus-external-target-writer:
        condition: service_completed_successfully
    ports:
      - "9090:9090"
    volumes:
      - ./cowork-monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./cowork-monitoring/prometheus/rules:/etc/prometheus/rules:ro
      - ./cowork-monitoring/prometheus/sd:/etc/prometheus/sd:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention.time=15d'
      - '--web.enable-lifecycle'
    extra_hosts:
      - "host.docker.internal:host-gateway"

  alertmanager-config-init:
    image: busybox:1.36
    container_name: cowork-alertmanager-config-init
    restart: "no"
    environment:
      DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL:-}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
    volumes:
      - ./cowork-monitoring/alertmanager/alertmanager.yml:/source/alertmanager.yml:ro
      - ./cowork-monitoring/alertmanager/alertmanager-noop.yml:/source/alertmanager-noop.yml:ro
      - alertmanager_config:/generated
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -eu
        umask 077
        rm -f /generated/alertmanager.yml /generated/discord-webhook-url

        if [ -n "$${DISCORD_WEBHOOK_URL}" ]; then
          cp /source/alertmanager.yml /generated/alertmanager.yml
          printf '%s' "$${DISCORD_WEBHOOK_URL}" > /generated/discord-webhook-url
          chown 65534:65534 /generated/discord-webhook-url
          chmod 0400 /generated/discord-webhook-url
        elif [ "$${SPRING_PROFILES_ACTIVE}" = "local" ]; then
          cp /source/alertmanager-noop.yml /generated/alertmanager.yml
        else
          echo "DISCORD_WEBHOOK_URL is required outside the local profile" >&2
          exit 1
        fi

        chmod 0444 /generated/alertmanager.yml

  alertmanager:
    image: prom/alertmanager:v0.33.0
    container_name: cowork-alertmanager
    restart: unless-stopped
    depends_on:
      alertmanager-config-init:
        condition: service_completed_successfully
    ports:
      - "9093:9093"
    volumes:
      - alertmanager_config:/etc/alertmanager/generated:ro
    command:
      - '--config.file=/etc/alertmanager/generated/alertmanager.yml'
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9093/-/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  grafana:
    image: grafana/grafana:13.0.2
    container_name: cowork-grafana
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./cowork-monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./cowork-monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus

  mysqld_exporter:
    image: prom/mysqld-exporter:v0.19.0
    container_name: cowork-mysqld-exporter
    restart: unless-stopped
    ports:
      - "9104:9104"
    environment:
      - MYSQLD_EXPORTER_PASSWORD=${MYSQL_ROOT_PASSWORD}
    command:
      - "--mysqld.address=mysql:3306"
      - "--mysqld.username=root"
    depends_on:
      mysql:
        condition: service_healthy

  redis_exporter:
    image: oliver006/redis_exporter:v1.86.0
    container_name: cowork-redis-exporter
    restart: unless-stopped
    ports:
      - "9121:9121"
    environment:
      - REDIS_ADDR=redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy

  kafka_exporter:
    image: danielqsj/kafka-exporter:v1.9.0
    container_name: cowork-kafka-exporter
    restart: unless-stopped
    ports:
      - "9308:9308"
    command:
      - "--kafka.server=kafka:9092"
    depends_on:
      kafka:
        condition: service_healthy

  postgres_exporter:
    image: prometheuscommunity/postgres-exporter:v0.19.1
    container_name: cowork-postgres-exporter
    restart: unless-stopped
    ports:
      - "9187:9187"
    environment:
      - DATA_SOURCE_NAME=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/postgres?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy

  mongodb_exporter:
    image: percona/mongodb_exporter:0.51.0
    container_name: cowork-mongodb-exporter
    restart: unless-stopped
    ports:
      - "9216:9216"
    environment:
      - MONGODB_URI=mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongodb:27017/admin
    depends_on:
      mongodb:
        condition: service_healthy

  # ── Application Services ───────────────────────────────────────────────────

  cowork-config:
    image: cowork-config:local
    container_name: cowork-config
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
      kafka-init:
        condition: service_completed_successfully
      vault-init:
        condition: service_completed_successfully
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      VAULT_HOST: cowork-vault
      MINIO_INTERNAL_ENDPOINT: ${MINIO_INTERNAL_ENDPOINT:-http://minio:9000}
      MINIO_PUBLIC_ENDPOINT: ${MINIO_PUBLIC_ENDPOINT:-http://localhost:9000}
      MINIO_PUBLIC_BASE_URL: ${MINIO_PUBLIC_BASE_URL:-http://localhost:9000/cowork-bucket}
    ports:
      - "${COWORK_CONFIG_HOST_PORT:-8761}:8761"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8761/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 90s

  cowork-gateway:
    image: cowork-gateway:local
    container_name: cowork-gateway
    restart: unless-stopped
    depends_on:
      cowork-config:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      SPRING_CONFIG_IMPORT: "configserver:http://cowork-config:8761"
      # TODO: 임시 - 홈서버 등 Eureka 미등록 외부 호스트 연동. ExternalRouteConfig 제거 시 함께 삭제할 것
      EXTERNAL_HOST_URL: ${EXTERNAL_HOST_URL:-}
      EXTERNAL_HOST_NAME: ${EXTERNAL_HOST_NAME:-external}
      EXTERNAL_HOST_HEALTH_PATH: ${EXTERNAL_HOST_HEALTH_PATH:-/health}
      EXTERNAL_HOST_DOCS_PATH: ${EXTERNAL_HOST_DOCS_PATH:-/v3/api-docs}
    ports:
      - "8080:8080"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 90s

  cowork-authorization:
    image: cowork-authorization:local
    container_name: cowork-authorization
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      kafka:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      APP_CONFIG_URL: http://cowork-config:8761
      APP_PROFILE: ${SPRING_PROFILES_ACTIVE:-local}
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  cowork-preference:
    image: cowork-preference:local
    container_name: cowork-preference
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      CONFIG_SERVER_URL: http://cowork-config:8761
    ports:
      - "9001:9001"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9001/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s

  cowork-user:
    image: cowork-user:local
    container_name: cowork-user
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      APP_CONFIG_URL: http://cowork-config:8761
      APP_PROFILE: ${SPRING_PROFILES_ACTIVE:-local}
    ports:
      - "8082:8082"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8082/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 90s

  cowork-team:
    image: cowork-team:local
    container_name: cowork-team
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      SPRING_CONFIG_IMPORT: "configserver:http://cowork-config:8761"
    ports:
      - "8085:8085"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8085/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 90s

  cowork-channel:
    image: cowork-channel:local
    container_name: cowork-channel
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      SPRING_CONFIG_IMPORT: "configserver:http://cowork-config:8761"
    ports:
      - "8083:8083"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8083/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 90s

  cowork-project:
    image: cowork-project:local
    container_name: cowork-project
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      SPRING_CONFIG_IMPORT: "configserver:http://cowork-config:8761"
    ports:
      - "${COWORK_PROJECT_HOST_PORT:-8084}:8084"
    volumes:
      - cowork_logs:/var/log/cowork
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8084/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 90s

  cowork-roadmap:
    image: cowork-roadmap:local
    container_name: cowork-roadmap
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      SPRING_CONFIG_IMPORT: "configserver:http://cowork-config:8761"
    ports:
      - "8088:8088"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8088/actuator/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 90s

  cowork-notification:
    image: cowork-notification:local
    container_name: cowork-notification
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      kafka:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      APP_CONFIG_URL: http://cowork-config:8761
      APP_PROFILE: ${SPRING_PROFILES_ACTIVE:-local}
    ports:
      - "8086:8086"
    secrets:
      - source: firebase_credentials
        target: firebase-credentials.json
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8086/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  cowork-chat:
    image: cowork-chat:local
    container_name: cowork-chat
    restart: unless-stopped
    depends_on:
      mongodb:
        condition: service_healthy
      kafka:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
      cowork-config:
        condition: service_healthy
    environment:
      APP_CONFIG_URL: http://cowork-config:8761
      APP_PROFILE: ${SPRING_PROFILES_ACTIVE:-local}
    ports:
      - "8087:8087"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8087/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 60s

  cowork-voice:
    image: cowork-voice:local
    container_name: cowork-voice
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
      kafka:
        condition: service_healthy
      livekit:
        condition: service_healthy
      cowork-channel:
        condition: service_healthy
    environment:
      APP_CONFIG_URL: http://cowork-config:8761
      APP_PROFILE: ${SPRING_PROFILES_ACTIVE:-local}
    ports:
      - "8089:8089"
    volumes:
      - cowork_logs:/var/log/cowork
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8089/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  mysql_data:
  postgres_data:
  mongo_data:
  kafka_data:
  redis_data:
  minio_data:
  es_data:
  prometheus_data:
  grafana_data:
  loki_data:
  cowork_logs:
  promtail_positions:
  alertmanager_config:

secrets:
  firebase_credentials:
    file: ./docker/secrets/firebase-credentials.json
