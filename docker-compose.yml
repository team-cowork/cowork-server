# Local development entry point. Production VM deployment: docs/deployment.md.
# Explicit paths keep build contexts and bind mounts relative to the repository.
include:
  - path:
      - deploy/compose/stack.yaml
      - deploy/compose/local.yaml
    project_directory: .
