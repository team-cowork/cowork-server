{
  "name": "cowork-promotion",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs public --watch",
    "build": "node scripts/build.mjs",
    "preview": "npm run build && node scripts/serve.mjs public"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.10.1",
    "yaml": "^2.9.0"
  }
}
