/**
 * 입력 문자열이 단일 이모지인지 검사하는 정규식.
 *
 * `RGI_Emoji` 유니코드 프로퍼티를 사용하므로 `/v` 플래그(ES2024)가 필요하다.
 * 국기(시퀀스), 스킨톤 변형, ZWJ 시퀀스 등 복합 이모지도 단일 이모지로 올바르게 판별한다.
 * 이모지 리액션 유효성 검증 시 "정확히 하나의 이모지"를 강제하기 위해 `^`·`$` 앵커를 사용한다.
 */
export const EMOJI_REGEX = /^\p{RGI_Emoji}$/v;
